source 'https://rubygems.org'
gem 'wdm', '>= 0.1.0' if Gem.win_platform?
group :jekyll_plugins do
    gem 'github-pages'
    gem 'jekyll-commonmark-ghpages'
    gem 'jekyll-livereload' # until github updates to jekyll >= 3.7.0
end
